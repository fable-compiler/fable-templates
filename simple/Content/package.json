{
  "private": true,
  "scripts": {
    "postinstall": "dotnet restore",
    "build": "cd src && dotnet fable webpack -- -p --config webpack.config.js",
    "start": "cd src && dotnet fable webpack-dev-server -- --config webpack.config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@babel/core": "^7.1.2",
    "fable-loader": "^2.0.0",
    "webpack": "^4.25.1",
    "webpack-cli": "^3.1.2",
    "webpack-dev-server": "^3.1.10"
  }
}
